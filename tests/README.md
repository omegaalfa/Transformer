# Test strategy

- `Unit/` will test isolated PHP contracts and implementations.
- `Integration/` will test boundaries between backends, models, and formats.
- `Parity/` will compare pure PHP, native execution, and external references.
- `Fixtures/` will hold small, reviewable numerical fixtures.

No placeholder test that always passes is included.
